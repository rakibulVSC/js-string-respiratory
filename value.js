const computer={
    brand:'lenovo',
    price:45000,
    processor:'ryzen',
    hdd:'512gb'
}
const keys=Object.keys(computer);
console.log(keys)
const valus=Object.values(computer);
console.log(valus)